<% object.forEach(review => { %>
<div class="row mb-3">
    <div class="col">
        <div class="card review">
            <div class="card-body">
            <h5 class="card-title">Rating: <%= review.rating %></h5>
                <h5 class="cart-title">Review: <%= review.review %></h5>
            </div>
        </div>
    </div>
</div>
<%});%>